import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Reset Password | Foodhub",
	description: "Reset password and use Foodhub for free.",
};

export default function ResetPasswordPage() {
	return <>Forgot Password</>;
}
